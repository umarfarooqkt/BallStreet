import React from 'react';
import ReactDOM from 'react-dom';
import { LeaguePage } from './leaguepage';

ReactDOM.render(
    <div>
        <LeaguePage/>
    </div>, document.getElementById('leaguePage')
);